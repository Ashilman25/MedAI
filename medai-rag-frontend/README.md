# MedAI‑RAG Frontend

React + Vite + Tailwind app for the MedAI‑RAG project (chat UI, sources panel, confidence).

## Run
```bash
npm install
npm install -D @vitejs/plugin-react
cp .env.example .env
# set VITE_API_BASE_URL=http://127.0.0.1:8000 and VITE_MOCK_MODE=false
npm run dev
```
